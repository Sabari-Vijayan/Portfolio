
import React from 'react';

/**
 * SpotifyStatus Component (Currently Inactive)
 * 
 * To reactivate, restore the previous logic that polls the /api/now-playing endpoint.
 */
const SpotifyStatus: React.FC = () => {
  // Return null to keep the component from rendering or performing any background tasks
  return null;
};

export default SpotifyStatus;
